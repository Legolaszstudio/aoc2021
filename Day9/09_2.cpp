#include <string>
#include <fstream>
#include <sstream>
#include <deque>
#include <vector>
#include <iostream>
#include "09_2.h"

void partTwo(std::vector<std::vector<int>> inputMatrix) {
	std::vector<std::vector<int>> concourMatrix(inputMatrix.size(), std::vector<int>(inputMatrix[0].size(), 0));
    
}